# Fit Tracker

Personal workout, nutrition and streak tracker. React + Vite, deployed to GitHub Pages.

- `npm install` then `npm run dev` to run locally
- Push to `main` and the GitHub Action builds and deploys to Pages
- Data is stored in the browser (localStorage) and optionally synced to a private GitHub Gist (Plan tab)
